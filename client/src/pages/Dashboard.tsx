import React from 'react';
import { Typography } from '@mui/material';

const Dashboard: React.FC = () => {
  return <Typography variant="h4">仪表盘（待实现）</Typography>;
};

export default Dashboard;