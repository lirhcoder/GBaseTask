import React from 'react';
import { Typography } from '@mui/material';

const Settings: React.FC = () => {
  return <Typography variant="h4">系统设置（待实现）</Typography>;
};

export default Settings;